// Custom Cursor: "Elastic String" (Clean Vector Line) - DISABLED
// Reverting to CSS-based cursor (white border circle) as per user request.

/*
document.addEventListener('DOMContentLoaded', () => {
    // Cleanup old cursor elements
    const oldCursors = document.querySelectorAll('.cursor-dot, .cursor-head, .cursor-segment, canvas.cursor-canvas');
    oldCursors.forEach(el => el.remove());

    // ... (rest of the code disabled)
});
*/
